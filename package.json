{
    "name": "my-app",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
      "build": "cd client && npm install && npm run build",
      "start": "cd server && npm install && npm run start"
    },
    "engines": {
      "node": "18.x"
    },
    "dependencies": {
      "express": "^4.18.2",
      "cookie-parser": "^1.4.6",
      "cors": "^2.8.5",
      "mysql2": "^3.6.0"
    }
  }